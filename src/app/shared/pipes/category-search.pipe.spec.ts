import { CategorySearchPipe } from './category-search.pipe';

describe('CategorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
