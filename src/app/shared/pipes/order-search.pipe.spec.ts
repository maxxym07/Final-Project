import { OrderSearchPipe } from './order-search.pipe';

describe('OrderSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
