import { SubcategorySearchPipe } from './subcategory-search.pipe';

describe('SubcategorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SubcategorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
